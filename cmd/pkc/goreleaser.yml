# goreleaser.yml
# Build customization
project_name: pkc

prerelease: true

build:
  binary: pkc
  goos:
    - windows
    - darwin
    - linux
  goarch:
    - amd64

# goreleaser.yml
brew:
  # Repository to push the tap to.
  github:
    owner: dpastoor 
    name: homebrew-tap 
